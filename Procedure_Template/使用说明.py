# ============================================================================
#  程序设计竞赛 — 代码框架使用说明
# ============================================================================
#  比赛时打开此文件作为参考，按步骤操作即可。
# ============================================================================

"""
══════════════════════════════════════════════════════════════════════════════
                           文件列表与职责
══════════════════════════════════════════════════════════════════════════════

  main_window.ui      用 Qt Designer 打开，可视化编辑界面
  calculator.py       手写 — 核心算法（工具函数 + 数据类 + 计算器类）
  file_io.py          手写 — 文件读取 parse_input + 写入 write_result
  app.py              手写 — 主程序（界面 + 算法 + 文件的调度中心）
  使用说明.py         本文件 — 操作指南（不参与运行）

══════════════════════════════════════════════════════════════════════════════
                   框架内嵌两套示例（均已注释）
══════════════════════════════════════════════════════════════════════════════

  示例A — 道路曲线要素计算（简单题）
    - 公式少（11条），无迭代，无数据类，单组输入，13项输出
    - calculator.py 中类名: Calculator_A
    - 适合: 大多数测绘计算题（坐标转换、导线计算、水准网平差...）

  示例B — 激光点云平面分割（复杂题）
    - 公式多（9+），有 RANSAC 迭代，有 3个数据类(Point/Grid/Plane)
    - 读入 1000 个点，多阶段计算(统计→栅格→S1→J1→J2→投影)，43项输出
    - calculator.py 中类名: Calculator_B
    - 适合: 涉及迭代、数据类、多阶段的复杂题目

  做你的题目时:
    1. 判断题目复杂度 → 选 A 或 B
    2. 取消对应示例的注释 → 跑通 → 确认框架无问题
    3. 把示例的公式/属性/输出项替换成你自己的
    4. 删除不需要的另一个示例，清理代码

══════════════════════════════════════════════════════════════════════════════
                     时间分配（4 小时）
══════════════════════════════════════════════════════════════════════════════

  简单题（选示例A）:
    0:00-0:10   读题，列输出清单+公式编号
    0:10-0:25   Qt Designer 修改 .ui
    0:25-0:28   pyuic5 生成 main_window_ui.py
    0:28-1:00   calculator.py（取消示例A→改公式）
    1:00-1:15   file_io.py（取消示例A→改格式）
    1:15-2:15   app.py（取消示例A→改槽函数+表格）
    2:15-3:30   集成测试+修bug（剩余时间充裕）
    3:30-4:00   收尾检查

  复杂题（选示例B）:
    0:00-0:15   读题，列输出清单+公式编号
    0:15-0:30   Qt Designer 修改 .ui
    0:30-0:33   pyuic5
    0:33-1:30   calculator.py（取消示例B→数据类+计算器类）
    1:30-1:55   file_io.py（取消示例B→多行解析）
    1:55-3:00   app.py（取消示例B→多阶段计算+43项填表）
    3:00-3:40   集成测试+修bug
    3:40-4:00   收尾

══════════════════════════════════════════════════════════════════════════════
              第1步：Qt Designer 修改界面（15-25 分钟）
══════════════════════════════════════════════════════════════════════════════

  1. 打开 Qt Designer → File → Open → main_window.ui

  2. 根据题目修改:

     a) 窗口标题: 对象查看器 → MainWindow → windowTitle
        <FILL: 改为你的题目名称>

     b) 左侧面板的控件（当前是示例A的布局）:
        - labelFile: 显示文件名
        - labelStatus: 显示数据摘要
        - 分隔线 (Line)
        - QFormLayout 中有 4 行输入框: JD里程 / 半径 / 偏角(度分秒) / 指定桩号

        根据你的题目:
          不需要的控件 → 在对象查看器中右键删除
          需要新的 QLabel → 从左侧控件栏拖入 → 设 objectName → 设 text
          需要新的 QLineEdit → 从左侧控件栏拖入 → 设 objectName → 设 placeholderText

        objectName 命名规则: 英文驼峰，有意义
          好: labelEll, editRadius, labelFile
          差: label1, lineEdit1, widget3

     c) 菜单动作（一般不用动）:
        actionOpen, actionCalc, actionSave, actionClear, actionExit
        如果题目需要"正算"和"反算"两个打开按钮:
          动作编辑器 → 新建 actionOpenDirect + actionOpenInverse
          拖入菜单和工具栏

     d) 完全不动: QSplitter, QTableWidget(tableResult),
        菜单栏结构, 工具栏结构, 状态栏

  3. Ctrl+S 保存

══════════════════════════════════════════════════════════════════════════════
          第2步：pyuic5 生成 main_window_ui.py（1 分钟）
══════════════════════════════════════════════════════════════════════════════

  在命令行中，切换到本目录，执行:

    pyuic5 -o main_window_ui.py main_window.ui

  如果 pyuic5 找不到，用:
    python -m PyQt5.uic.pyuic -o main_window_ui.py main_window.ui

  生成后不要手动编辑 main_window_ui.py!
  改界面 = 回 Qt Designer 改 .ui → 重新 pyuic5

══════════════════════════════════════════════════════════════════════════════
               第3步：写 calculator.py
══════════════════════════════════════════════════════════════════════════════

  打开 calculator.py，决定用示例A还是B。

  ╔══════════════════════════════════════════════════════════════╗
  ║  选示例A（简单）:                                            ║
  ║    1. 删掉示例B的3个数据类 + Calculator_B 整块              ║
  ║    2. 取消 Part1 中 dms_to_decimal + normalize_angle 的注释 ║
  ║    3. 取消 Calculator_A 全部注释                             ║
  ║    4. 改名为 Calculator                                     ║
  ║    5. 替换 __init__ 的输入参数和输出属性                     ║
  ║    6. 替换子步骤中的公式                                    ║
  ║    7. 调整 compute_all 的调用顺序                           ║
  ╚══════════════════════════════════════════════════════════════╝

  ╔══════════════════════════════════════════════════════════════╗
  ║  选示例B（复杂）:                                            ║
  ║    1. 删掉 Calculator_A 整块                                ║
  ║    2. 取消 Part1 工具函数中需要的函数注释                   ║
  ║    3. 取消 Part2 三个数据类(Point/Grid/Plane)的注释         ║
  ║    4. 取消 Calculator_B 全部注释                             ║
  ║    5. 改名为 Calculator                                     ║
  ║    6. 修改数据类属性 → 修改 __init__ 输出属性               ║
  ║    7. 修改子步骤方法中的公式                                ║
  ║    8. 调整 run() 中的调用顺序                               ║
  ╚══════════════════════════════════════════════════════════════╝

  【通用要点】
    - 输出属性全部在 __init__ 中声明（初始化为 0.0 / None / []）
    - 子步骤按"被依赖的先写"从上到下排列
    - compute_all / run 中按依赖顺序调用
    - 三角函数输入 = 弧度（math.radians），显示时 = 度（math.degrees）
    - 只 import math，不用 numpy

══════════════════════════════════════════════════════════════════════════════
               第4步：写 file_io.py
══════════════════════════════════════════════════════════════════════════════

  打开 file_io.py，两个函数填。

  parse_input — 读文件:
    框架提供了 3 种格式示例（均已注释）:
      A: 单行制表符+逗号（道路曲线）
      B: 多行逗号+数据类（点云）
      大地主题: 多行+字符标记过滤+模式识别
    选最接近的，取消注释，修改。

    过滤字符标记(P1,P2...)的通用技巧:
      for t in tokens:
          try:
              numbers.append(float(t))
          except ValueError:
              pass   # 不能转 float 的自动跳过

  write_result — 写文件:
    rows = [(序号, "指标名", f"{值:.Nf}"), ...]
    取消对应示例的注释 → 替换成你的输出清单
    底部 4 行通用写入代码照抄不动。

══════════════════════════════════════════════════════════════════════════════
               第5步：写 app.py
══════════════════════════════════════════════════════════════════════════════

  打开 app.py，逐个槽函数填空。

  【完全照抄不动】
    _setup_table()             3列表格设置
    _connect_signals()         5个信号连接
    slot_save 的 try/except    只改对话框标题+write_result调用
    slot_clear 的结构          只改 QLabel 复位列表
    __main__ 入口              4行

  【需要填空】
    slot_open:
      调 file_io.parse_input → 更新左侧 QLabel
      path.split('/')[-1] 取文件名

    slot_calc:
      构造 Calculator → compute_all / run → 存到 _results

    slot_save:
      对话框标题 + 默认文件名
      调 file_io.write_result(path, self._results)

    slot_clear:
      复位所有 QLabel.setTexit("...") + QLineEdit.setTexit("")

    _fill_table:
      rows = [...]  每个元素的第2项（指标名称）必须与试题册逐字一致
      底部填表代码（setRowCount → 循环 setItem）照抄不动

  【单组 vs 多组】
    单组: self._results = calc（一个对象）
    多组: self._results = [calc1, calc2, ...]（列表）
          _fill_table 中 for i, r in enumerate(...) 逐组拼装

══════════════════════════════════════════════════════════════════════════════
               第6步：集成测试
══════════════════════════════════════════════════════════════════════════════

  1. 先确认框架能跑（验证你的环境没问题）:
     - 取消示例A的所有注释
     - 创建 curve_input.txt，内容: 1\t1326.480,280,32,16,42
     - python app.py → 打开文件 → 计算 → 13项结果 → 保存

  2. 替换成你的题目后:
     [ ] 打开文件 → 左侧标签正确显示
     [ ] 计算 → 表格正确填充，无 Python 异常
     [ ] 保存 → .txt 格式正确 (序号,指标名,值)
     [ ] 清除 → 所有标签/表格复位
     [ ] 不打开直接点计算 → 警告弹窗
     [ ] 不计算直接点保存 → 警告弹窗
     [ ] 计算结果与手算一致

══════════════════════════════════════════════════════════════════════════════
         控件速查: .ui 中的 objectName ↔ app.py 中的访问
══════════════════════════════════════════════════════════════════════════════

  QAction            →  self.ui.actionOpen / actionCalc / actionSave / ...
  QLabel             →  self.ui.labelFile.setText("文字")
  QLineEdit          →  self.ui.edit_jd.text() 获取 / setText("") 设置
  QTableWidget       →  self.ui.tableResult
  新建的控件          →  self.ui.<你在Qt Designer中设的objectName>

══════════════════════════════════════════════════════════════════════════════
                          常见问题 FAQ
══════════════════════════════════════════════════════════════════════════════

  Q: 怎么判断选示例A还是示例B?
  A: 看试题册有没有"数据类"（Point/Line/Plane之类）和"迭代"（RANSAC/牛顿法）。
     没有 → 示例A。有 → 示例B。

  Q: Qt Designer 在哪里?
  A: pip install pyqt5-tools，在开始菜单搜索 "Qt Designer"。

  Q: pyuic5 命令找不到?
  A: python -m PyQt5.uic.pyuic -o main_window_ui.py main_window.ui

  Q: 运行报 "No module named 'main_window_ui'"?
  A: 还没执行 pyuic5。回到第2步。

  Q: 运行报 "AttributeError: object has no attribute 'xxx'"?
  A: app.py 中 self.ui.xxx 和 .ui 中的 objectName 不一致。
     去 Qt Designer 确认那个控件的 objectName。

  Q: 表格空白不显示数据?
  A: 检查 _fill_table 中 rows 是否为 [ ]（示例没取消注释）。
     或者 setRowCount 在 setItem 之后调了（必须先 setRowCount）。

  Q: 只剩 1 小时，界面还没做完?
  A: 优先保证 calculator.py + file_io.py 正确。
     命令行验证: python -c "from calculator import Calculator; c=Calculator(...); c.compute_all(); print(c.result)"
     PyQt5 界面写个最简单的（一个窗口+一个按钮+一个 TextEdit）能跑就行。
     算法分 > 界面分。

  Q: 示例全取消注释后报错?
  A: 两个 Calculator 类同时定义了同名方法，只能保留一个。
     删掉不需要的那个示例再跑。
"""

# 祝考试顺利！
