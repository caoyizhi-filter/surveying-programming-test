# ============================================================
# 使用说明 —— 遥感影像最大似然法分类 单文件程序
# ============================================================
# 本文件是纯文档，不可运行。运行程序请用 miaoda_code.py。
# ============================================================

"""
═══════════════════════════════════════════════════════════
一、文件清单
═══════════════════════════════════════════════════════════
  miaoda_code.py     ← 唯一的程序文件，包含全部计算逻辑 + GUI 界面
  train.txt          训练样本数据
  pixel.txt          待分类像元数据
  verify.txt         精度验证样本数据
  mlc_result.txt     程序运行后自动生成的结果文件

═══════════════════════════════════════════════════════════
二、数据文件格式
═══════════════════════════════════════════════════════════
  逗号分隔，每行一条记录：

  train.txt  →  类别ID, 波段1值, 波段2值
    示例：     1,41.92,45.67
               1,43.10,46.20
               2,42.50,44.80

  pixel.txt  →  波段1值, 波段2值（无类别ID，因为待分类）
    示例：     41.50,45.30
               42.10,44.90

  verify.txt →  真实类别ID, 波段1值, 波段2值
    示例：     1,42.10,44.90
               2,43.80,45.90

═══════════════════════════════════════════════════════════
三、运行方式
═══════════════════════════════════════════════════════════

  ▸ 方式一：纯计算模式（当前默认，无需 GUI 依赖）
    ─────────────────────────────────────────────
    打开终端，进入本目录，执行：
      python miaoda_code.py

    效果：
      - 控制台打印：类别1像元数、类别2像元数、OA、Kappa
      - 自动生成 mlc_result.txt，内容为 17 项完整计算结果

    适用场景：比赛时只求计算结果、不需要展示图形界面。


  ▸ 方式二：GUI 图形界面模式（需要 PyQt5）
    ─────────────────────────────────────
    步骤：
      ① 打开 miaoda_code.py，翻到文件末尾（最后约 15 行）
      ② 把「入口A」注释掉，把「入口B」取消注释，变成：

         # --- 入口A：纯计算模式 ---
         # if __name__ == "__main__":
         #     r = run_all()
         #     ...

         # --- 入口B：GUI界面模式 ---
         if __name__ == "__main__":
             app = QApplication(sys.argv)
             win = App()
             win.show()
             sys.exit(app.exec_())

      ③ 运行：python miaoda_code.py
      ④ 在窗口中点击工具栏按钮选择三个数据文件
      ⑤ 点击「计算」按钮
      ⑥ 结果表格自动填充，可点击「导出」保存为 CSV

    适用场景：需要直观查看结果、或比赛要求提交带界面的程序。


  ▸ 方式三：在其他 Python 脚本中导入使用
    ──────────────────────────────────
    from miaoda_code import run_all

    # 使用默认文件名（当前目录下的 train.txt / pixel.txt / verify.txt）
    result = run_all()

    # 或指定文件路径
    result = run_all(
        train_path  = "data/train.txt",
        pixel_path  = "data/pixel.txt",
        verify_path = "data/verify.txt",
        output_path = "output/result.txt"
    )

    # 获取结果
    print(result["OA"])           # 总体精度，如 0.6667
    print(result["KAP"])          # Kappa 系数，如 0.3333
    print(result["count_cls1"])   # 分为类别1的像元数
    print(result["count_cls2"])   # 分为类别2的像元数

    # 获取表格数据（可直接给 GUI 渲染）
    for no, label, value in result["table_rows"]:
        print(f"{no}. {label} = {value}")


═══════════════════════════════════════════════════════════
四、代码结构总览（自上而下 7 部分）
═══════════════════════════════════════════════════════════

  导入模块             math, sys, PyQt5（QtWidgets + QtCore）
  ─────────────────────────────────────────────────────
  第一部分（6个函数）    向量/矩阵基础运算
     vec_sub()       向量减法            → 计算 x-μ
     vec_dot()       向量内积            → 二次型最终标量化
     vec_mul_T()     外积 v@vᵀ          → 协方差累加基础
     mat_add()       矩阵加法            → 累加外积矩阵
     mat_scalar()    矩阵数乘            → 无偏估计 ×1/(N-1)
     vec_mat_mul()   行向量×矩阵         → dxᵀ @ Σ⁻¹ 中间步骤
    ★ 换任何竞赛题都无需修改。这些是通用线性代数工具。
  ─────────────────────────────────────────────────────
  第二部分（2个函数）    矩阵高级运算
     mat_det()       高斯消元求行列式 |Σ|
     mat_inv()       高斯-约当求逆矩阵 Σ⁻¹
    ★ 换任何竞赛题都无需修改。
  ─────────────────────────────────────────────────────
  第三部分（3个函数）    文件读取
     read_train()    读取训练样本 → {类别ID: [样本向量列表]}
     read_pixel()    读取待分类像元 → [向量列表]
     read_verify()   读取验证样本 → [(真实类别, 向量), ...]
    ★ 文件格式变化时修改 split 分隔符和列解析逻辑。
  ─────────────────────────────────────────────────────
  第四部分（2个函数）    参数计算
     calc_mean()          均值向量 μ
     calc_cov_unbiased()  无偏协方差矩阵 Σ（分母 N-1）
    ★ 换任何竞赛题都无需修改。
  ─────────────────────────────────────────────────────
  第五部分（1个函数）    核心公式
     calc_g()        判别值 g(x) = ln|Σ| + (x-μ)ᵀΣ⁻¹(x-μ)
    ★ 换任何竞赛题都无需修改。
  ─────────────────────────────────────────────────────
  第六部分（3个函数）    精度评估 + 主流程
     build_confusion()  混淆矩阵构建
     calc_oa_kappa()    OA 和 Kappa 计算
     run_all()          主流程入口 ★★★ 换题重写这个函数 ★★★
  ─────────────────────────────────────────────────────
  第七部分（1个类）    GUI 界面
     class App(QMainWindow)   主窗口
     FILE_CONFIG              输入文件配置 ★★ 换题改这里 ★★
  ─────────────────────────────────────────────────────
  程序入口              两套入口，注释切换


═══════════════════════════════════════════════════════════
五、run_all() 详解 —— 最核心的函数
═══════════════════════════════════════════════════════════

  ▸ 函数签名
    def run_all(train_path  = "train.txt",
                pixel_path  = "pixel.txt",
                verify_path = "verify.txt",
                output_path = "mlc_result.txt"):

    四个参数都有默认值，不传参则读取当前目录下的默认文件。


  ▸ 函数内部流程（5 步）
    步骤1：调用 read_train / read_pixel / read_verify 读取数据
    步骤2：调用 calc_mean / calc_cov_unbiased 算两类别的 μ 和 Σ
    步骤3：遍历所有待分类像元，分别算 g1 和 g2，g 小的为预测类别
    步骤4：调用 build_confusion / calc_oa_kappa 做精度评估
    步骤5：组装 table_rows（17 项指标），写入文件，返回结果字典


  ▸ 返回值详解
    result = run_all(...)

    result 是一个字典，包含以下字段：

    字段名           类型      说明
    ──────────────────────────────────────────────────
    table_rows       list      GUI 表格直接使用的数据源
                               格式：[(序号, 指标名, 值), ...]
                               共 17 项，每项是三元组
    count_cls1       int       分为类别1 的像元总数
    count_cls2       int       分为类别2 的像元总数
    OA               float     总体分类精度（0~1）
    KAP              float     Kappa 系数（-1~1）

    其中 table_rows 是跟 GUI 的「数据契约」——
    GUI 不关心表格里是什么内容，只管遍历这个 list 填充单元格。
    所以换题时只要新 run_all() 的返回格式不变，GUI 代码零改动。


  ▸ table_rows 的当前内容（本题 17 项）
    序号    指标名称                         示例值
    ────────────────────────────────────────────────
    1       类别 1 训练样本总数              16
    2       类别 1 波段 1 均值               42.2712
    3       类别 1 波段 2 均值               45.2900
    4       类别 1 协方差矩阵行列式           0.645397
    5       类别 2 训练样本总数              16
    6       类别 2 波段 1 均值               42.5244
    7       类别 2 波段 2 均值               45.2263
    8       类别 2 协方差矩阵行列式           2.245037
    9       第 1 个待分类像元 类别1 对数似然  -0.216618
    10      第 1 个待分类像元 类别2 对数似然  1.489238
    11      第 1 个待分类像元最终分类类别     1
    12      验证样本总数量                    18
    13      混淆矩阵对角线正确样本总数        12
    14      总体分类精度 OA                   0.6667
    15      分类 Kappa 系数                   0.3333
    16      所有待分类像元中类别1总个数       8
    17      所有待分类像元中类别2总个数       22


═══════════════════════════════════════════════════════════
六、换一道竞赛题，具体改哪里？
═══════════════════════════════════════════════════════════

  全文共有 6 处需要关注，分别标注了【竞赛题切换点】：

  ┌──────┬──────────────────────────┬──────────────────────────────┐
  │ 编号  │ 位置（行号附近）           │ 改什么                        │
  ├──────┼──────────────────────────┼──────────────────────────────┤
  │  ①   │ run_all() 函数体          │ 整个流程逻辑重写               │
  │      │ (第6部分，约第260行)       │ 类别数、波段数、输出项全在这里  │
  ├──────┼──────────────────────────┼──────────────────────────────┤
  │  ②   │ FILE_CONFIG 列表          │ 输入文件的数量/标签/默认文件名  │
  │      │ (第7部分顶部)              │ 增删改条目即可                 │
  ├──────┼──────────────────────────┼──────────────────────────────┤
  │  ③   │ OUTPUT_FILE 变量          │ 默认输出文件名                 │
  │      │ (第7部分顶部)              │                               │
  ├──────┼──────────────────────────┼──────────────────────────────┤
  │  ④   │ setWindowTitle()          │ 窗口标题文字                  │
  │      │ (App.__init__ 中)          │                               │
  ├──────┼──────────────────────────┼──────────────────────────────┤
  │  ⑤   │ _calc() 中 run_all() 调用  │ 传参个数/顺序跟签名匹配        │
  │      │ (第7部分)                  │                               │
  ├──────┼──────────────────────────┼──────────────────────────────┤
  │  ⑥   │ _calc() 中状态栏 showMessage│ 字段名改成新返回值中的字段     │
  │      │ (第7部分)                  │                               │
  └──────┴──────────────────────────┴──────────────────────────────┘


  ▸ 改动详解①：重写 run_all() —— 最关键的改动
    ────────────────────────────────────────
    run_all() 是题目特定的。换题时内部的类别数、波段数、
    输出项全部重写。但必须保持以下两条不变：

    【不变1】函数签名可以根据需要调整参数，但返回值必须包含：
      {
          "table_rows":  [(序号, 指标名称, 值字符串), ...],
          ...            ← 其他字段自由增减
      }

    【不变2】table_rows 中每个元素是 (int, str, str) 三元组：
      (序号, 指标名称, 计算结果)

    示例：如果新题目有 3 个类别、4 个波段，run_all 重写为：
    ┌────────────────────────────────────────────────────┐
    │ def run_all(data_path="data.txt",                  │
    │             output_path="result.txt"):              │
    │     # 读取数据（调用修改后的 read 函数）              │
    │     # 计算 3 类的均值 μ 和协方差 Σ                   │
    │     # 对每个像元算 3 个 g 值，取最小的分类            │
    │     # 精度评估                                       │
    │                                                      │
    │     table_rows = [                                   │
    │         (1, "类别1 样本数", str(n1)),                 │
    │         (2, "类别1 波段1均值", f"{mu1[0]:.4f}"),     │
    │         (3, "类别1 波段2均值", f"{mu1[1]:.4f}"),     │
    │         (4, "类别1 波段3均值", f"{mu1[2]:.4f}"),     │
    │         (5, "类别1 波段4均值", f"{mu1[3]:.4f}"),     │
    │         ...                                          │
    │         (N, "OA", f"{OA:.4f}"),                      │
    │     ]                                                │
    │                                                      │
    │     with open(output_path, "w") as f:                 │
    │         ...                                          │
    │                                                      │
    │     return {                                         │
    │         "table_rows": table_rows,                    │
    │         "OA": OA, "KAP": KAP,                       │
    │         ...   # 新题目需要的其他字段                  │
    │     }                                                │
    └────────────────────────────────────────────────────┘


  ▸ 改动详解②③：FILE_CONFIG 和 OUTPUT_FILE
    ──────────────────────────────────────
    当前：
      FILE_CONFIG = [
          ("train",  "训练样本",   "train.txt"),
          ("pixels", "待分类像元", "pixel.txt"),
          ("verify", "验证样本",   "verify.txt"),
      ]
      OUTPUT_FILE = "mlc_result.txt"

    新题改为（例如只需要两个文件）：
      FILE_CONFIG = [
          ("samples", "训练数据", "samples.txt"),
          ("tests",   "测试数据", "tests.txt"),
      ]
      OUTPUT_FILE = "result.txt"

    改完这两处，菜单栏、工具栏、文件对话框全部自动适配新配置，
    不需要修改任何其他 GUI 代码。


  ▸ 改动详解④⑤⑥：窗口和 _calc 方法
    ─────────────────────────────
    ④ setWindowTitle("新题目名称")

    ⑤ _calc() 方法中的 run_all 调用，参数名要跟 FILE_CONFIG 的 key 对应：
       r = run_all(self._paths["samples"], self._paths["tests"], OUTPUT_FILE)

    ⑥ 状态栏显示内容，字段名改成新 run_all 返回值里的：
       self.statusBar().showMessage(
           f"完成 | OA={r['OA']:.4f} | Kappa={r['KAP']:.4f}")


  ▸ 改动详解（补充）：文件读取函数（仅当文件格式变化时）
    ──────────────────────────────────────────
    如果新题目的数据文件分隔符不是逗号（如空格），或列含义不同，
    修改第三部分的 read_train / read_pixel / read_verify。

    例如分隔符从逗号改为空格：
      将  line.split(",")  改为  line.split()

    例如波段数从 2 变为 4：
      无需改代码。parts[1:] 自动取第 2 列到最后一列，波段数自适应。


═══════════════════════════════════════════════════════════
七、常见问题
═══════════════════════════════════════════════════════════

  Q: 为什么控制台输出中文乱码？
  A: Windows 终端的 GBK 编码问题，不影响程序正确性。
     打开 mlc_result.txt，里面的中文是正常的 UTF-8。

  Q: run_all() 可否脱离 GUI 单独使用？
  A: 完全可以。from miaoda_code import run_all 然后调用即可。
     run_all 只依赖 math 模块，不依赖任何 PyQt5 代码。

  Q: GUI 界面能否直接展示给评委看？
  A: 可以，切换到入口B 启动 GUI 模式即可。窗口支持文件选择、
     一键计算、结果表格、导出 CSV 全部操作。

  Q: 如果想增加新的输出项怎么办？
  A: 在 run_all() 的 table_rows 列表中追加新条目即可，
     GUI 自动多显示一行，不需要改任何 GUI 代码。

  Q: 依赖是什么？
  A: 纯计算模式 → 零依赖，Python 3 自带 math/sys 就行。
     GUI 模式   → 需要 pip install pyqt5（约 50MB）。


═══════════════════════════════════════════════════════════
八、所有重要函数参考手册
═══════════════════════════════════════════════════════════

  ▍第一部分：向量/矩阵基础运算（6个）—— 永不修改 ▍

  ┌─ vec_sub(v1, v2) ─────────────────────────────────┐
  │ 功能：向量减法，v1 - v2，对应元素相减               │
  │ 参数：v1 (list) 被减向量                           │
  │       v2 (list) 减数向量，长度必须与 v1 相同         │
  │ 返回：list，长度与输入相同                          │
  │ 数学：计算偏差 dx = x - μ，用于后续协方差和判别值    │
  │ 示例：vec_sub([5,8], [2,3]) → [3, 5]              │
  │ 调用者：calc_cov_unbiased, calc_g                  │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘

  ┌─ vec_dot(v1, v2) ─────────────────────────────────┐
  │ 功能：向量内积（点积），Σ(v1[i] × v2[i])            │
  │ 参数：v1, v2 两个等长 list                         │
  │ 返回：float 标量                                   │
  │ 数学：二次型的最后一步，将中间向量与 dx 做内积       │
  │       得到最终的马氏距离平方值                       │
  │ 示例：vec_dot([1,2], [3,4]) → 1×3+2×4 = 11.0     │
  │ 调用者：calc_g                                     │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘

  ┌─ vec_mul_T(v) ────────────────────────────────────┐
  │ 功能：列向量 × 自身转置，生成外积方阵 v @ vᵀ        │
  │ 参数：v (list) 长度为 n 的一维向量（波段数）         │
  │ 返回：n×n 的二维 list（外积矩阵）                   │
  │ 数学：协方差计算的原子操作，(x-μ)(x-μ)ᵀ             │
  │       结果[i][j] = v[i] × v[j]                     │
  │ 示例：vec_mul_T([a,b]) → [[a²,ab],[ab,b²]]        │
  │ 调用者：calc_cov_unbiased                          │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘

  ┌─ mat_add(m1, m2) ─────────────────────────────────┐
  │ 功能：矩阵加法，两个同阶方阵对应位置相加             │
  │ 参数：m1, m2 两个 n×n 二维 list                    │
  │ 返回：n×n 二维 list                                │
  │ 数学：将所有训练样本的外积矩阵累加求和               │
  │ 示例：mat_add([[1,2],[3,4]], [[5,6],[7,8]])        │
  │       → [[6,8],[10,12]]                            │
  │ 调用者：calc_cov_unbiased                          │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘

  ┌─ mat_scalar(mat, k) ──────────────────────────────┐
  │ 功能：矩阵数乘，每个元素 × 常数 k                   │
  │ 参数：mat 二维 list, k 标量                         │
  │ 返回：二维 list，每个元素 ×k                        │
  │ 数学：外积总和 ÷ (N-1)，即乘以 1/(N-1)，             │
  │       得到无偏协方差估计（贝塞尔校正）               │
  │ 示例：mat_scalar([[1,2],[3,4]], 0.5) →             │
  │       [[0.5,1.0],[1.5,2.0]]                        │
  │ 调用者：calc_cov_unbiased                          │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘

  ┌─ vec_mat_mul(vec, mat) ───────────────────────────┐
  │ 功能：行向量 × 矩阵，vecᵀ @ mat                     │
  │ 参数：vec (list) 长度 n 的行向量                    │
  │       mat (list) n×n 二维矩阵                      │
  │ 返回：list 长度 n 的行向量                          │
  │ 数学：计算 dxᵀ @ Σ⁻¹，即偏差行向量乘逆协方差矩阵     │
  │ 示例：vec_mat_mul([1,2], [[3,4],[5,6]]) →         │
  │       [1×3+2×5, 1×4+2×6] = [13, 16]               │
  │ 调用者：calc_g                                     │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘


  ▍第二部分：矩阵高级运算（2个）—— 永不修改 ▍

  ┌─ mat_det(mat) ────────────────────────────────────┐
  │ 功能：高斯消元法求方阵行列式 |Σ|                     │
  │ 参数：mat 二维方阵                                  │
  │ 返回：float，行列式值。奇异矩阵返回 0.0              │
  │ 算法：列选主元 → 行交换(det×-1) → 主元归一化         │
  │       (det×主元值) → 消去下方行                     │
  │ 数学：行列式衡量协方差矩阵的"体积"，用于 ln|Σ|       │
  │ 注意：函数内部会深拷贝矩阵，不会修改传入的原始数据    │
  │ 调用者：calc_g, run_all (求 det1, det2)            │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘

  ┌─ mat_inv(mat) ────────────────────────────────────┐
  │ 功能：高斯-约当增广矩阵法求逆矩阵 Σ⁻¹               │
  │ 参数：mat n×n 可逆方阵                              │
  │ 返回：n×n 二维 list，即 mat 的逆矩阵                │
  │ 算法：构造 n×2n 增广矩阵 [mat|I] → 行变换使左侧      │
  │       变单位阵 → 右侧即为逆矩阵                      │
  │ 异常：主元 < 1e-12 时抛出 Exception("矩阵奇异")     │
  │ 数学：逆矩阵用于马氏距离计算，校正波段间相关性        │
  │ 调用者：calc_g                                     │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘


  ▍第三部分：文件读取（3个）—— 文件格式变化时修改 ▍

  ┌─ read_train(filepath) ────────────────────────────┐
  │ 功能：读取训练样本文件，按类别分组                   │
  │ 参数：filepath (str) 文件路径                       │
  │ 返回：dict {类别ID: [样本向量列表]}                  │
  │       例：{1: [[41.9,45.7],[43.1,46.2]],           │
  │             2: [[42.5,44.8],[43.8,45.9]]}          │
  │ 格式：每行 "类别ID,波段1,波段2", 逗号分隔            │
  │ 调用者：run_all                                    │
  │ 切换：分隔符或列含义变化时修改                        │
  └────────────────────────────────────────────────────┘

  ┌─ read_pixel(filepath) ────────────────────────────┐
  │ 功能：读取待分类像元文件                             │
  │ 参数：filepath (str) 文件路径                       │
  │ 返回：list [[波段1,波段2,...], ...]                  │
  │       无类别标签，因为类别正是我们要预测的            │
  │ 格式：每行 "波段1,波段2", 逗号分隔                   │
  │ 调用者：run_all                                    │
  │ 切换：分隔符或波段数变化时修改                        │
  └────────────────────────────────────────────────────┘

  ┌─ read_verify(filepath) ───────────────────────────┐
  │ 功能：读取精度验证样本文件                           │
  │ 参数：filepath (str) 文件路径                       │
  │ 返回：list [(真实类别, 特征向量), ...]               │
  │       例：[(1, [42.1,44.9]), (2, [43.8,45.9])]    │
  │ 格式：每行 "真实类别ID,波段1,波段2", 逗号分隔        │
  │ 调用者：run_all                                    │
  │ 切换：分隔符或列含义变化时修改                        │
  └────────────────────────────────────────────────────┘


  ▍第四部分：参数计算（2个）—— 永不修改 ▍

  ┌─ calc_mean(sample_list) ──────────────────────────┐
  │ 功能：计算一类样本的均值向量 μ                        │
  │ 参数：sample_list (list) 该类所有样本向量的列表       │
  │ 返回：list 长度=波段数，μ = [μ₁, μ₂, ...]            │
  │ 公式：μ = (1/N) × Σ xⱼ（对每个波段分别求平均）       │
  │ 示例：calc_mean([[1,2],[3,4],[5,6]]) → [3.0, 4.0] │
  │ 调用者：run_all                                    │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘

  ┌─ calc_cov_unbiased(sample_list, mean_vec) ────────┐
  │ 功能：计算无偏协方差矩阵 Σ                            │
  │ 参数：sample_list 样本列表                          │
  │       mean_vec     该类均值向量                     │
  │ 返回：n×n 二维 list                                │
  │ 公式：Σ = 1/(N-1) × Σ (x-μ)(x-μ)ᵀ                 │
  │ 步骤：遍历样本 → 算偏差 vec_sub → 外积 vec_mul_T     │
  │       → 累加 mat_add → 数乘 mat_scalar              │
  │ 注意：分母用 N-1 而非 N（贝塞尔校正，无偏估计）       │
  │ 调用者：run_all                                    │
  │ 改否：永不修改                                      │
  └────────────────────────────────────────────────────┘


  ▍第五部分：核心公式（1个）—— 永不修改 ▍

  ┌─ calc_g(x, mu, cov) ──────────────────────────────┐
  │ 功能：计算像元 x 对某类的对数似然判别值               │
  │ 参数：x   (list) 待分类像元特征向量                  │
  │       mu  (list) 该类均值向量                       │
  │       cov (list) 该类协方差矩阵                      │
  │ 返回：float，判别值 g(x)。越小 → 概率越大            │
  │ 公式：g(x) = ln|Σ| + (x-μ)ᵀ Σ⁻¹ (x-μ)             │
  │       第1项 ln|Σ|：协方差"体积"惩罚                  │
  │       第2项 二次型：马氏距离的平方                    │
  │ 异常：如果 |Σ| ≈ 0，抛出异常"矩阵奇异"               │
  │ 调用者：run_all, build_confusion                   │
  │ 改否：永不修改                                      │
  │ 说明：这就是最大似然法的全部数学，换任何竞赛题         │
  │       只要还是最大似然法，这个函数就不用改。           │
  └────────────────────────────────────────────────────┘


  ▍第六部分：精度评估 + 主流程（3个）—— 主流程换题重写 ▍

  ┌─ build_confusion(verify_data, mean_dict, cov_dict) ┐
  │ 功能：遍历验证样本构建混淆矩阵                        │
  │ 参数：verify_data   [(真实类, 向量), ...]            │
  │       mean_dict     {类ID: 均值向量}                 │
  │       cov_dict      {类ID: 协方差矩阵}               │
  │ 返回：(confusion, idx_map)                          │
  │       confusion: n×n 二维 list，行=真实，列=预测     │
  │       idx_map:   {类别ID: 行列索引}                  │
  │ 算法：对每个验证样本，用 calc_g 计算各类 g 值，        │
  │       取 g 最小者为预测类别，与真实类别比较            │
  │ 调用者：run_all                                    │
  │ 改否：无需修改（类别数自适应）                         │
  └────────────────────────────────────────────────────┘

  ┌─ calc_oa_kappa(conf_mat) ─────────────────────────┐
  │ 功能：从混淆矩阵计算 OA 和 Kappa 系数                │
  │ 参数：conf_mat n×n 混淆矩阵                         │
  │ 返回：(正确数, 总数, OA, Kappa)                      │
  │       OA    = Σ对角线 / Σ全部                        │
  │       Kappa = (OA - Pe) / (1 - Pe)                  │
  │       Pe = Σ(行和×列和) / N²（随机期望一致率）        │
  │ 调用者：run_all                                    │
  │ 改否：无需修改（类别数自适应）                         │
  └────────────────────────────────────────────────────┘

  ┌─ run_all(train_path, pixel_path, verify_path,      ┐
  │          output_path)                                │
  │ 功能：执行完整最大似然分类流程 ★★★ 最核心函数 ★★★     │
  │ 参数：四个路径字符串，均有默认值                       │
  │       train_path  = "train.txt"                      │
  │       pixel_path  = "pixel.txt"                      │
  │       verify_path = "verify.txt"                     │
  │       output_path = "mlc_result.txt"                 │
  │ 返回：dict {                                         │
  │         "table_rows": [(序号,指标名,值),...],          │
  │         "count_cls1": int, "count_cls2": int,        │
  │         "OA": float, "KAP": float                    │
  │       }                                              │
  │ 内部流程（5步）：                                     │
  │   ① 调三个 read 函数读取数据                          │
  │   ② 调 calc_mean + calc_cov_unbiased 算参数          │
  │   ③ 遍历像元，对每类调 calc_g，g 小的为预测类别       │
  │   ④ 调 build_confusion + calc_oa_kappa 精度评估     │
  │   ⑤ 组装 table_rows，写文件，返回结果                 │
  │ 调用者：程序入口、GUI._calc、外部脚本                 │
  │ 切换：★ 换题时重写本函数，保持返回格式不变 ★           │
  │ 关键：table_rows 是跟 GUI 的数据契约——                │
  │       只要返回的 table_rows 格式不变，GUI 完全不用改    │
  └────────────────────────────────────────────────────┘


  ▍第七部分：GUI 界面（1个关键类 + 2个配置） ▍

  ┌─ FILE_CONFIG（列表常量）────────────────────────────┐
  │ 功能：定义输入文件的配置，驱动整个文件选择器 UI        │
  │ 格式：[(内部key, 界面标签, 默认文件名), ...]          │
  │ 当前：[("train","训练样本","train.txt"),              │
  │        ("pixels","待分类像元","pixel.txt"),           │
  │        ("verify","验证样本","verify.txt")]            │
  │ 切换：★ 增删改条目即可，UI 全自动适配 ★               │
  └────────────────────────────────────────────────────┘

  ┌─ OUTPUT_FILE（字符串常量）──────────────────────────┐
  │ 功能：默认输出文件名                                  │
  │ 当前："mlc_result.txt"                               │
  │ 切换：★ 改成你想要的默认文件名 ★                      │
  └────────────────────────────────────────────────────┘

  ┌─ class App(QMainWindow) ───────────────────────────┐
  │ 功能：PyQt5 主窗口，提供完整的 GUI 操作界面           │
  │ 窗口：1100×720，标题见 setWindowTitle               │
  │ 布局：左侧文件状态面板 + 右侧结果表格（可拖拽分割）    │
  │                                                      │
  │ 关键方法：                                           │
  │   __init__()          初始化窗口、配置、搭建 UI       │
  │   _setup_actions()    创建菜单/工具栏的动作对象       │
  │                       由 FILE_CONFIG 驱动，通用       │
  │   _setup_menubar()    组装菜单栏                      │
  │                       文件项由 FILE_CONFIG 驱动       │
  │   _setup_toolbar()    组装工具栏                      │
  │                       文件项由 FILE_CONFIG 驱动       │
  │   _setup_central()    中央区域：左侧面板+右侧表格      │
  │                       表格 3 列：序号|说明|计算结果    │
  │   _open(kind)         打开文件对话框                  │
  │                       由 FILE_CONFIG 驱动，通用       │
  │   _calc()             执行计算并刷新表格 ★            │
  │                       调用 run_all → 取 table_rows    │
  │                       → 遍历填表 → 更新状态栏         │
  │                       此处需跟 run_all 签名匹配        │
  │   _save()             导出为 CSV 文件                 │
  │                       通用逻辑，无需修改               │
  │   _clear()            重置所有状态                    │
  │                       通用逻辑，无需修改               │
  │                                                      │
  │ 设计原则：                                           │
  │   GUI 完全不接触计算逻辑，只认识 run_all() 返回的     │
  │   table_rows。换一道竞赛题，只要 run_all 返回格式     │
  │   不变，GUI 代码零改动。                              │
  │                                                      │
  │ 切换：★ 只改 FILE_CONFIG + OUTPUT_FILE + 窗口标题     │
  │         + _calc 中 run_all 调用 + 状态栏字段 ★        │
  └────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════
九、函数调用关系图
═══════════════════════════════════════════════════════════

  程序入口 ──→ run_all()
                  │
                  ├──→ read_train()     返回训练数据
                  ├──→ read_pixel()     返回待分类像元
                  ├──→ read_verify()    返回验证数据
                  │
                  ├──→ calc_mean()      计算各类均值 μ
                  ├──→ calc_cov_unbiased()
                  │      ├──→ vec_sub()        偏差 x-μ
                  │      ├──→ vec_mul_T()      外积 (x-μ)(x-μ)ᵀ
                  │      ├──→ mat_add()        累加外积
                  │      └──→ mat_scalar()     ×1/(N-1)
                  │
                  ├──→ calc_g()          对每个像元算判别值
                  │      ├──→ mat_det()        行列式 |Σ|
                  │      ├──→ mat_inv()        逆矩阵 Σ⁻¹
                  │      ├──→ vec_sub()        偏差 x-μ
                  │      ├──→ vec_mat_mul()    dxᵀ @ Σ⁻¹
                  │      └──→ vec_dot()        最终内积标量
                  │
                  ├──→ build_confusion()
                  │      └──→ calc_g()         对验证样本分类
                  │
                  └──→ calc_oa_kappa()    计算 OA 和 Kappa

  GUI.App._calc() ──→ run_all() ──→ 取 table_rows → 填表

  关键点：所有箭头最终汇聚到 run_all()。
         换题时只改 run_all() 内部流程 + table_rows 内容。
         底层数学函数不需要懂，也不需要改。
"""

